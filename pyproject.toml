[build-system]
requires = ["uv_build >= 0.11.7, <0.12.0"]
build-backend = "uv_build"


[project]
name = "utilities_etl"
version = "0.0.1"
description = "Reusable utilities for ETL and database connections"
requires-python = ">=3.12"
authors = [
  {name = "UJJWOL KC"}
]
dependencies = [
    
]


[dependency-groups]
dev = [
    "pandas==2.2.3",
    "psycopg2==2.9.11", 
    "sqlalchemy==2.0.48", 
    "requests==2.32.5", 
    "py-countries-states-cities-database==2.2.10",
    "countrystatecity-countries==1.0.2",
    "pycountry==26.2.16",
    "snowflake-connector-python[pandas]==4.6.0",
    "dotenv==0.9.9",
    "pip",
    "black"
  ]

[tool.uv.build-backend]
module-name = "utilities"
module-root = ""
